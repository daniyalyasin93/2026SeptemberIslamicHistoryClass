# TIMELINE — every date used anywhere, with its citation

Single source of truth for dates. Nothing goes on a slide, in a script or on a printable
until it appears here with a source. `(to verify)` is allowed; a bare guess is not.

| Event | ھ | عیسوی | Source | Used in |
|---|---|---|---|---|
| _populated at build time_ | | | | |
