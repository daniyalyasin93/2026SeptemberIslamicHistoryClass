# PowerPoint lock files
~$*.pptx
~$*.docx
# Python
__pycache__/
*.pyc
# Local scratch — per global convention, .claude notes are NOT committed
.claude/
series/data/
_*.png
sources/pdf/*
sources/archive*
# research notes render to PDF for reading (tools/render_note.py --all); the .md is canonical
docs/research/.render/
docs/research/*.pdf

# cached auto-trimmed copies of visual assets (series/deck2.py)
series/visuals/*.trim.png

# per-slide PNG exports from series/preview.py
*_preview/
*_sheet.png
docs/research/read/

# video clips are too large for the repo; decks that embed them are not committed either
*.mp4

# review artefacts from S03_yemen/review_dump.py
S03_yemen/.build/sheets/
S03_yemen/.build/deck_dump.txt
